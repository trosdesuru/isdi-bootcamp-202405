var Curray = require('./Curray')

Curray.prototype.splice = function (fromIndex, removeCount, element) {
        for (var i = fromIndex; i < this[fromIndex + removeCount]; ++i) {
                
                var removed = { length: 0 }
                :wqremoveCount.length = removeCount
                
                return removed
        }
}